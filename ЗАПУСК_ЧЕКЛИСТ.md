# ✅ ЧЕК-ЛИСТ ЗАПУСКА САЙТА ООО "ЭТАЖИ"

## 📦 ПОДГОТОВКА (ВЫПОЛНЕНО)

- [x] ✅ Проверены все файлы проекта
- [x] ✅ Создан .env файл для продакшена
- [x] ✅ Сгенерирован новый SECRET_KEY
- [x] ✅ Создана инструкция DEPLOYMENT.md
- [x] ✅ Создана резервная копия базы (etazhi_backup.db)
- [x] ✅ Создан ZIP-архив для переноса (lift_site_deploy.zip)
- [x] ✅ Все страницы проверены и работают

---

## 🚀 ЗАПУСК НА СЕРВЕРЕ (ЗАВТРА)

### Шаг 1: Копирование проекта на сервер

```bash
# Вариант A: Через SCP
scp lift_site_deploy.zip user@your-server:/var/www/

# Вариант B: Через Git
git clone <repository-url> /var/www/lift_site

# Вариант C: Через FTP/SFTP
# Используйте FileZilla или WinSCP
```

---

### Шаг 2: Установка Python и зависимостей

```bash
# Обновление пакетов
sudo apt update && sudo apt upgrade -y

# Установка Python 3.9+
sudo apt install python3 python3-pip python3-venv -y

# Проверка версии
python3 --version  # Должна быть 3.9 или выше

# Переход в директорию проекта
cd /var/www/lift_site

# Создание виртуального окружения
python3 -m venv venv

# Активация виртуального окружения
source venv/bin/activate

# Обновление pip
pip install --upgrade pip

# Установка зависимостей
pip install -r requirements.txt

# Установка Gunicorn для продакшена
pip install gunicorn
```

---

### Шаг 3: Настройка .env файла

```bash
# Перейдите в директорию проекта
cd /var/www/lift_site

# Отредактируйте .env файл
nano .env
```

**ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ:**

```env
# 1. Пароль от почты Mail.ru (используйте пароль приложения!)
MAIL_PASSWORD=ваш_пароль_приложения_mail_ru

# 2. Убедитесь, что отладка отключена
FLASK_DEBUG=False
FLASK_ENV=production

# 3. SECRET_KEY уже сгенерирован (можно оставить текущий)
SECRET_KEY=c92d9cafa6e13851d6ffaadbc82cc3282fadc77ab5357d9d2135eacd074795c3
```

**Как получить пароль приложения Mail.ru:**
1. Зайдите на почту https://mail.ru
2. Настройки (шестерёнка) → Безопасность
3. Включите двухфакторную аутентификацию
4. Прокрутите вниз до "Пароли для внешних приложений"
5. Создайте новый пароль для "Почта"
6. Скопируйте и вставьте в .env

---

### Шаг 4: Запуск Gunicorn/Waitress

**Вариант A: Gunicorn (Linux, рекомендуется)**

```bash
# Активируйте виртуальное окружение
source venv/bin/activate

# Тестовый запуск
gunicorn -w 4 -b 0.0.0.0:5000 app:app

# Проверьте в браузере: http://ваш-server:5000
```

**Вариант B: Waitress (Windows)**

```batch
venv\Scripts\activate
waitress-serve --port=5000 app:app
```

---

### Шаг 5: Настройка Nginx (опционально, рекомендуется)

```bash
# Установка Nginx
sudo apt install nginx -y

# Создание конфигурационного файла
sudo nano /etc/nginx/sites-available/lift_site
```

**Вставьте конфигурацию:**

```nginx
server {
    listen 80;
    server_name etazhi-lifts.online www.etazhi-lifts.online;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /static {
        alias /var/www/lift_site/static;
        expires 30d;
    }
}
```

**Активация:**

```bash
# Создание символической ссылки
sudo ln -s /etc/nginx/sites-available/lift_site /etc/nginx/sites-enabled/

# Проверка конфигурации
sudo nginx -t

# Перезапуск Nginx
sudo systemctl restart nginx
sudo systemctl enable nginx
```

---

### Шаг 6: Настройка SSL сертификата (HTTPS)

```bash
# Установка Certbot
sudo apt install certbot python3-certbot-nginx -y

# Получение сертификата
sudo certbot --nginx -d etazhi-lifts.online -d www.etazhi-lifts.online

# Следуйте инструкциям Certbot
# Введите email для уведомлений
# Примите условия использования
# Выберите редирект с HTTP на HTTPS (рекомендуется)
```

**Проверка HTTPS:**
- Откройте https://etazhi-lifts.online
- Проверьте, что замок отображается в браузере

**Проверьте все страницы:**

- [ ] **Главная**: https://etazhi-lifts.online/
  - Проверьте логотип, статистику, услуги
  
- [ ] **Услуги**: https://etazhi-lifts.online/services
  - Проверьте все 4 услуги
  
- [ ] **Контакты**: https://etazhi-lifts.online/contacts
  - Проверьте форму обратной связи
  - Отправьте тестовое сообщение
  
- [ ] **Админка**: https://etazhi-lifts.online/admin
  - Войдите в панель
  - Проверьте получение заявки

**Проверка email уведомлений:**
1. Отправьте тестовую заявку через форму контактов
2. Проверьте почту daniillomeyko@mail.ru
3. Убедитесь, что письмо получено

**Проверка логов:**

```bash
# Логи приложения
tail -f /var/www/lift_site/etazhi.log

# Логи Nginx
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log

# Логи Gunicorn (если через systemd)
journalctl -u lift_site -f
```

---

## 🔧 АВТОЗАПУСК ПРИЛОЖЕНИЯ (Systemd)

**Создание службы:**

```bash
sudo nano /etc/systemd/system/lift_site.service
```

**Вставьте содержимое:**

```ini
[Unit]
Description=ООО ЭТАЖИ - Сайт компании
After=network.target

[Service]
User=www-data
Group=www-data
WorkingDirectory=/var/www/lift_site
Environment="PATH=/var/www/lift_site/venv/bin"
ExecStart=/var/www/lift_site/venv/bin/gunicorn -w 4 -b 127.0.0.1:5000 app:app
Restart=always

[Install]
WantedBy=multi-user.target
```

**Активация:**

```bash
sudo systemctl daemon-reload
sudo systemctl start lift_site
sudo systemctl enable lift_site
sudo systemctl status lift_site
```

---

## 📊 МОНИТОРИНГ

**Проверка статуса:**

```bash
# Статус службы
sudo systemctl status lift_site

# Статус Nginx
sudo systemctl status nginx

# Проверка портов
sudo netstat -tulpn | grep :5000
sudo netstat -tulpn | grep :80
sudo netstat -tulpn | grep :443
```

**Резервное копирование:**

```bash
# Создание бэкапа базы данных
cp /var/www/lift_site/etazhi.db /var/www/lift_site/etazhi_backup_$(date +%Y%m%d).db

# Настройка автоматического бэкапа (cron)
crontab -e

# Добавьте строку для ежедневного бэкапа в 2:00
0 2 * * * cp /var/www/lift_site/etazhi.db /var/www/lift_site/backups/etazhi_$(date +\%Y\%m\%d).db
```

---

## 🆘 ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК

**Сайт не загружается:**

```bash
# Проверьте статус службы
sudo systemctl status lift_site

# Проверьте логи
sudo journalctl -u lift_site -n 50

# Проверьте, слушает ли порт
sudo netstat -tulpn | grep 5000

# Перезапустите службу
sudo systemctl restart lift_site
```

**Ошибка базы данных:**

```bash
# Проверьте права доступа
ls -la /var/www/lift_site/etazhi.db

# Исправьте права
sudo chown www-data:www-data /var/www/lift_site/etazhi.db
```

**Email не отправляется:**

1. Проверьте лог: `tail -f /var/www/lift_site/etazhi.log`
2. Убедитесь, что пароль приложения Mail.ru правильный
3. Проверьте настройки SMTP в .env
4. Убедитесь, что двухфакторная аутентификация включена

**Ошибки SSL:**

```bash
# Проверьте сертификат
sudo certbot certificates

# Обновите сертификат
sudo certbot renew

# Проверьте конфигурацию Nginx
sudo nginx -t
```

---

## 📞 КОНТАКТЫ ДЛЯ ПОДДЕРЖКИ

**Файлы логов:**
- Приложение: `/var/www/lift_site/etazhi.log`
- Nginx: `/var/log/nginx/`
- Systemd: `journalctl -u lift_site`

**Важные пути:**
- Проект: `/var/www/lift_site/`
- База данных: `/var/www/lift_site/etazhi.db`
- Конфиг Nginx: `/etc/nginx/sites-available/lift_site`
- Служба: `/etc/systemd/system/lift_site.service`

---

## ✅ ФИНАЛЬНАЯ ПРОВЕРКА

Перед тем как считать сайт запущенным, убедитесь:

- [ ] Сайт доступен по HTTPS
- [ ] Все 4 страницы работают (Главная, Услуги, Контакты, Админка)
- [ ] Форма контактов отправляет заявки
- [ ] Email уведомления приходят на daniillomeyko@mail.ru
- [ ] Админ-панель показывает заявки
- [ ] Сервис автозапуска настроен
- [ ] Резервное копирование настроено
- [ ] Логи записываются корректно

---

**Дата подготовки:** 2026
**Проект:** ООО "ЭТАЖИ" - lift_site
**Версия:** 1.0

---

## 🎯 БЫСТРАЯ КОМАНДА ДЛЯ ПРОВЕРКИ ВСЕГО

```bash
echo "=== СТАТУС ПРИЛОЖЕНИЯ ===" && \
sudo systemctl status lift_site --no-pager && \
echo "" && \
echo "=== СТАТУС NGINX ===" && \
sudo systemctl status nginx --no-pager && \
echo "" && \
echo "=== ПОРТЫ ===" && \
sudo netstat -tulpn | grep -E ':(5000|80|443)' && \
echo "" && \
echo "=== ПОСЛЕДНИЕ ЛОГИ ===" && \
tail -20 /var/www/lift_site/etazhi.log
```

---

**УДАЧНОГО ЗАПУСКА! 🚀**
